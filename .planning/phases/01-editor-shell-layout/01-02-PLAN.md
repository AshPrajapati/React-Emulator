---
phase: 01-editor-shell-layout
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/components/SplitPane.jsx, src/components/SplitPane.css, src/App.jsx, src/App.css]
autonomous: false

must_haves:
  truths:
    - "User sees split-pane layout with editor left, preview placeholder right"
    - "User can resize the split pane by dragging divider"
    - "Layout maintains state during resizing"
  artifacts:
    - path: "src/components/SplitPane.jsx"
      provides: "Resizable split-pane component"
      exports: ["SplitPane"]
    - path: "src/App.jsx"
      provides: "App with split-pane layout"
      contains: "SplitPane"
  key_links:
    - from: "src/components/SplitPane.jsx"
      to: "Editor and Preview panes"
      via: "children props"
      pattern: "children\\[0\\].*children\\[1\\]"
---

<objective>
Implement resizable split-pane layout and verify the complete editor shell

Purpose: Complete the Phase 1 UI with professional split-pane interface
Output: Working editor with resizable split-pane layout ready for preview integration
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-editor-shell-layout/01-CONTEXT.md
@.planning/phases/01-editor-shell-layout/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Split Pane component</name>
  <files>src/components/SplitPane.jsx, src/components/SplitPane.css</files>
  <action>
    Create a custom resizable split-pane component:

    1. Create SplitPane.jsx:
       - Use useState to track split position (default 50%)
       - Render two panes side-by-side with a draggable divider
       - Handle mouse events for dragging:
         - onMouseDown on divider starts drag
         - Track mouse movement to update split position
         - onMouseUp ends drag
       - Children[0] goes in left pane, children[1] in right pane
       - Apply flex or width based on split percentage

    2. Create SplitPane.css:
       - Style the container with display: flex
       - Style the divider (4-8px wide, visible hover state, cursor: col-resize)
       - Ensure panes can collapse fully (min-width: 0)
       - Match dark theme from editor

    3. Handle edge cases:
       - Prevent text selection during drag
       - Allow full collapse to either side
       - Maintain position on window resize
  </action>
  <verify>ls src/components/SplitPane.jsx && grep "onMouseDown" src/components/SplitPane.jsx</verify>
  <done>SplitPane component exists with mouse event handlers for resizing</done>
</task>

<task type="auto">
  <name>Task 2: Integrate split pane in App</name>
  <files>src/App.jsx, src/App.css</files>
  <action>
    Update App to use SplitPane layout:

    1. Import SplitPane component

    2. Wrap Editor in left pane and placeholder in right pane:
       - Left pane: Editor component
       - Right pane: Dark placeholder div with "Preview" text

    3. Create App.css:
       - Set app container to full viewport (100vh, 100vw)
       - Remove default margins/padding
       - Style preview placeholder:
         - Dark background matching editor theme
         - Centered "Preview" text
         - Same background color as editor

    4. Ensure layout fills entire viewport without scrollbars
  </action>
  <verify>grep "SplitPane" src/App.jsx && grep "Preview" src/App.jsx</verify>
  <done>App uses SplitPane with Editor on left and preview placeholder on right</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 editor shell with Monaco editor and resizable split-pane layout</what-built>
  <how-to-verify>
    1. Open browser to http://localhost:5173
    2. Verify Monaco editor appears on the left with dark theme
    3. Verify preview placeholder appears on the right
    4. Try dragging the divider between panes:
       - Should resize smoothly
       - Should allow collapsing either pane fully
    5. Type some JSX code in the editor
    6. Verify syntax highlighting works for JSX
  </how-to-verify>
  <resume-signal>Type "approved" if the split-pane layout works correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Split-pane layout fills viewport
- Divider is draggable and resizes panes
- Editor remains functional during/after resize
- Can collapse panes fully to either side
- Dark theme consistent across both panes
</verification>

<success_criteria>
- [ ] Split-pane layout with editor left, preview right
- [ ] Draggable divider resizes panes smoothly
- [ ] Can collapse either pane completely
- [ ] Monaco editor still works after resizing
- [ ] UI looks professional with consistent dark theme
</success_criteria>

<output>
After completion, create `.planning/phases/01-editor-shell-layout/01-02-SUMMARY.md`
</output>